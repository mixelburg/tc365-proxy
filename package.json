{
  "name": "tc365-proxy",
  "version": "3.0.0",
  "description": "TimeClock 365 auto-punch Telegram bot — users register once, the bot punches them in/out on a per-user daily schedule and pings them (TypeScript, zero runtime deps).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "keygen": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
